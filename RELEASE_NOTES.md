# v0.1.1

This release updates manuscript-ordered methods scripts and improves open-source reproducibility docs.

Changes:
- Update CellChat analysis script for the cell communication step.
- Update Monocle2 trajectory analysis scripts.
- Add `dotplot.R` helper and make `1.R` a wrapper that sources it (for pathway scoring).
- Keep only the complete LMBDs scoring script; remove the redundant MBDs analysis script.
- Keep only the RCC2 functional exploration script; remove the redundant RCC2 script.
- Update `README.md` entrypoints and reproducibility notes.
